// Source: https://usaco.guide/general/io

#include <bits/stdc++.h>
#include <cstdio>
#include <iterator>
using namespace std;

int main() {
	freopen("teleport.in", 'r', cin);
    freopen("teleport.out", 'w', cout);

    int a;
    int b;
    int c;
    int d;

    cin >> a >> b >> c >> d;

    int nums[4] = {a, b, c, d};

    std:sort(nums, nums + size(nums));


}
